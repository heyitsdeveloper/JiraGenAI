                                                                       \nfrom selenium import webdriver\nfrom selenium.webdriver.common.keys import Keys\n\n# Initialize the Chrome driver\ndriver = webdriver.Chrome()\n\n# Navigate to the Admin UI page\ndriver.get('https://admin-ui:8080')\n\n# Locate and click the 'Change Wifi Password' button\nchange_password_button = driver.find_element_by_xpath('//button[contains(text(),'Change Wifi Password')]')\nchange_password_button.click()\n\n# Enter the current wifi password\ncurrent_password_input = driver.find_element_by_name('currentPassword')\ncurrent_password_input.send_keys('password')\n\n# Enter the new wifi password\nnew_password_input = driver.find_element_by_name('newPassword')\nnew_password_input.send_keys('StrongPassword123')\n\n# Click the 'Save' button\nsave_button = driver.find_element_by_xpath('//button[contains(text(),'Save')]')]')\nsave_button.click()\n\n# Close the driver\ndriver.quit()